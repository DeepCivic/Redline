// @redline/redline-shared — zod schemas shared between the UI and adapters.
// Populated from Thread 2 onwards. Placeholder keeps the build graph coherent.
export const PROC_SHARED_PLACEHOLDER = true;
